# AGENTS.md — conventions for AI coding agents

This is the single source of truth for how to extend this codebase. Claude Code,
Cursor, Codex, etc. should read this first. Human-oriented docs live in `docs/`.

## What this is

A production-grade Flutter **template** for building mobile apps. It runs with
**zero API keys** out of the box (mock/no-op service impls). Swap mocks for real
backends/services without touching UI or domain code.

## Golden rules

1. **Architecture is layered and one-directional:** `presentation → domain ← data`.
   - `domain/` = entities + repository *interfaces*. No Flutter, no packages.
   - `data/` = `*_data_source.dart`, DTOs (`fromJson`/`toJson`), repository impls.
   - `presentation/` = Cubits (`flutter_bloc`) + pages/widgets.
   - **Never** import `data/` from `presentation/`, or Flutter from `domain/`.
2. **Errors never throw to the UI.** Data sources throw `AppException`s; repositories
   wrap calls in `guardAsync` and return `Result<T>` (`Ok`/`Err`). Cubits `fold` the
   result into states. See `lib/core/error/`.
3. **No code generation.** No `build_runner`/`freezed`/`json_serializable`. Write DTOs
   and `copyWith` by hand. (Localizations use the built-in `flutter gen-l10n`, which is
   fine.)
4. **State = Cubit.** Sealed state classes extending `Equatable`. Model
   loading/loaded/empty/error explicitly.
5. **DI is manual `get_it`.** Register everything in `lib/core/di/injector.dart`.
   Global state → `registerLazySingleton`; per-screen cubits → `registerFactory`.
6. **Strings are localized.** Add keys to **all** ARB files in
   `lib/core/l10n/arb/` (`en`, `de`, `es`, `ar`). `en` is the template. Use
   `context.l10n.<key>`.
7. **No secrets in code.** Config comes from `AppConfig` (`--dart-define`). Tokens go in
   `SecureStore`, never `KeyValueStore`.

## Recipe: add a feature (copy `features/example_notes/`)

1. `domain/<x>.dart` — entity (`Equatable` + `copyWith`).
2. `domain/<x>_repository.dart` — abstract interface returning `Result<T>`.
3. `data/dto/<x>_dto.dart` — `fromJson`/`toJson`/`toEntity`/`fromEntity`.
4. `data/<x>_data_source.dart` + a `mock_<x>_data_source.dart` (throws `AppException`s).
5. `data/<x>_repository_impl.dart` — wrap calls in `guardAsync`.
6. `presentation/cubit/<x>_state.dart` (sealed) + `<x>_cubit.dart`.
7. `presentation/pages/...` — `BlocProvider(create: () => getIt<XCubit>()..load())`.
8. Register repo + cubits in `injector.dart`.
9. Add a route in `core/router/routes.dart` + `app_router.dart`.
10. Add ARB keys to **all** locales.
11. Add a `blocTest` in `test/features/<x>/`.

## Routing

`go_router` with a pure redirect guard (`redirectGuard` in `app_router.dart`) driven by
`AuthCubit` + the `onboardingDone` flag. Don't emit `AuthLoading` outside startup — it
routes to splash. Navigate with `context.goNamed`/`pushNamed(Routes.x)`.

## Services (gated, mock-default)

`lib/services/{ads,purchases,notifications}` each expose an interface with a
mock/no-op default and a real impl chosen in `injector.dart` based on `AppConfig`
flags. Never call a real SDK's `init()`/`configure()` unless the flag/key is set.

## DO NOT TOUCH / DO NOT COMMIT

- `lib/core/l10n/gen/` — generated; run `flutter gen-l10n` instead.
- Platform folders (`android/`, `ios/`, …) — generated by `flutter create .`.
- `dart_define*.json` (except `.example`), signing files, `google-services.json`,
  `GoogleService-Info.plist` — secrets; gitignored.

## Removing the example feature

`features/example_notes/` is a reference, not a requirement. To start clean: delete the
folder, remove its routes from `core/router/`, its DI lines from `injector.dart`, and
its tests. Point `Routes.notes` (the post-auth home) at your own first screen.

## Before you finish

Run: `flutter analyze` (must be clean) and `flutter test`. Keep `dart format` happy.
