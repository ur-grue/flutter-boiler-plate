# /spec — single source of truth
From APPFACTORY_INPUTS.md (ask at most 3 follow-ups), write APP_SPEC.md.

Seed it with the **aso-skills** strategy skills (method, not memory):
- `app-marketing-context` → establish the app's positioning context up front.
- `category-positioning` → which store category + how we position within it.
- `monetization-strategy` → the revenue model (informs the monetization section below).
- `onboarding-optimization` + `retention-optimization` → shape the onboarding flow + core
  loop so they convert and retain (feeds the screen list).

Write: problem (vetted format), niche, core loop (trigger→input→processing→output),
screen list, monetization (monthly/yearly -50%/lifetime, trial, paywall after
onboarding, entitlement "premium"), brand keywords.
If it's an AI app, also output the edge-function system prompt + strict JSON schema
(runs server-side, never in the client). Follow AGENTS.md.
