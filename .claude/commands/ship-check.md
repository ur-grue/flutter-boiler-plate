# /ship-check — pre-submit gate → PASS/FAIL
Verify: analyze clean; test green; account deletion present; legal links open;
ATT prompt if tracking; privacy-label/data-safety answers ready; paywall shows
price + restore. Then run the design-critic subagent on the latest screenshots
and list the top 5 fixes. Output PASS or FAIL.
